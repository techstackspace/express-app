{
  "name": "express-app",
  "module": "src/app.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/app.ts",
    "check": "tsc --noEmit",
    "build": "bun build src/app.ts --outdir dist",
    "start": "bun run dist/app.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.0.14"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
